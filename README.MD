# DearAlarm

一个基于 **Dear ImGui + GLFW + OpenGL3** 的轻量级闹铃。

---

## 📦 项目结构概览

```
.
├── main.cpp                # 主程序入口
├── CMakeLists.txt          # CMake 构建脚本
├── app_icon.ico / .png     # 应用图标（用于窗口和托盘）
├── resources.rc            # Windows 资源文件（嵌入图标等）
├── imgui.ini               # ImGui 布局配置（运行时生成）
├── thirdparty/             # 第三方依赖库
│   ├── imgui/              # Dear ImGui 源码
│   ├── miniaudio.h         # 音频播放（用于 alarm.mp3）
│   └── stb_image.h         # 图像加载（用于 app_icon.png）
└── build/                  # 构建输出目录（由 CMake 生成）
```

---

## 🛠️ 构建说明

### 依赖
- C++17 编译器，GCC
- CMake ≥ 4.2.0
- GLFW
- OpenGL
- Windows 平台

### 构建步骤（Windows + MinGW 示例）
```powershell
mkdir build && cd build
cmake .. -G "MinGW Makefiles"
mingw32-make
```
构建成功后，可执行文件位于 `build/DearAlarm.exe`。

---

## 🚀 快速分发

只需打包以下 **3 个文件** 即可运行：

```
DearAlarm.exe    # 主程序（假设为静态链接）
app_icon.png     # 程序运行时动态加载的图标
alarm.mp3        # 提醒音效（代码中硬编码路径）
```

> 💡 注意：`alarm.mp3` 必须与 `DearAlarm.exe` 位于同一目录，否则无法播放提醒音！

---

## 🎵 进阶功能特点
- 托盘常驻，点击弹出设置界面
- 可设置多个定时提醒
- 支持自定义提醒音（当前固定为 `alarm.mp3`）
- 跨平台基础架构（当前仅在 Windows 测试）

---

## 📝 许可证
GPLv3

---

> Made with ❤️ and Dear ImGui  
> © 2026 CiCi-usx
